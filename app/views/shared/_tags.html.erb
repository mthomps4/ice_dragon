<div class="flex flex-col gap-2 my-2 dark:text-white">
  <h2 class="text-lg font-bold">Tags</h2>
  <% @tags.each do |tag| %>
    <%= link_to blog_path(q: { tags_id_eq: tag.id }), data: { turbo_frame: "_top" } do %>
      <%= tag.name %>
    <% end %>
  <% end %>
</div>